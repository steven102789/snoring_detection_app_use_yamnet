<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    tools:context=".fragment.LogFragment">

    <!-- TODO: Update blank fragment layout -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="20"
        >
        <ImageButton
            android:id="@+id/previousDay"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_marginRight="2dp"
            android:background="@drawable/arrowleft3"
            android:layout_alignParentBottom="true"
            android:contentDescription="left date"
            android:layout_toLeftOf="@+id/calendar"
            />
        <TextView
            android:id="@+id/calendar"
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:text=""
            android:gravity="center"
            android:textSize="30sp"
            android:textColor="@color/white"
            android:layout_centerHorizontal="true"
            android:layout_alignParentBottom="true"/>
        <ImageButton
            android:id="@+id/nextDay"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:background="@drawable/arrowright3"
            android:layout_alignParentBottom="true"
            android:layout_marginRight="2dp"
            android:contentDescription="right date"
            android:layout_toRightOf="@+id/calendar"
            />

    </RelativeLayout>
    <ListView
        android:id="@+id/data"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="80"
        android:layout_marginRight="25dp"
        android:layout_marginLeft="25dp"
        android:layout_marginTop="25dp"
        android:layout_marginBottom="25dp"
        android:background="@drawable/rectangle"/>

</LinearLayout>